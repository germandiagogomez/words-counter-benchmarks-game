fmt_dep = dependency('fmt', version: '>=9.0', include_type: 'system')

words_counter_prog = executable('words_counter',
                                sources: ['words_counter.cpp'],
                                dependencies: [count_words_dep, fmt_dep])
